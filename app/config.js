/* ============================================================
   SplitFace — app configuration
   ------------------------------------------------------------
   PASTE YOUR PAYMENT LINK BELOW.
   Muhammad: replace the placeholder with your real Gumroad or
   Lemon Squeezy checkout URL (the $4.99 one-time "Remove
   watermark + HD" product). Example:
     PAYMENT_URL: "https://gumroad.com/l/splitface-pro"
   Every "Go Pro / Remove watermark" button in the app opens
   this URL in a new tab. Defined once here — nowhere else.
   ============================================================ */
window.SPLITFACE_CONFIG = {
  PAYMENT_URL: "https://www.example.com/splitface-pro-checkout",
};
